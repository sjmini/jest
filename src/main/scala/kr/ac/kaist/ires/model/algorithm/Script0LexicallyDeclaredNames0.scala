package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._
import scala.collection.immutable.{ Map, HashMap, HashSet }

object Script0LexicallyDeclaredNames0 {
  val length: Int = 0
  val func: Func = Func("""Script0LexicallyDeclaredNames0""", List(), None, IReturn(EList(List())).setId(47079))
  val instToStepMap: Map[Int, Int] = Map(47079 -> 0, 47080 -> 0)
  val stepToInstsMap: Map[Int, HashSet[Int]] = Map(0 -> HashSet(47079, 47080))
  /* Beautified form:
  "Script0LexicallyDeclaredNames0" () => return (new [])
  */
}
