package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._
import scala.collection.immutable.{ Map, HashMap, HashSet }

object Script0LexicallyScopedDeclarations0 {
  val length: Int = 0
  val func: Func = Func("""Script0LexicallyScopedDeclarations0""", List(), None, IReturn(EList(List())).setId(47089))
  val instToStepMap: Map[Int, Int] = Map(47089 -> 0, 47090 -> 0)
  val stepToInstsMap: Map[Int, HashSet[Int]] = Map(0 -> HashSet(47090, 47089))
  /* Beautified form:
  "Script0LexicallyScopedDeclarations0" () => return (new [])
  */
}
