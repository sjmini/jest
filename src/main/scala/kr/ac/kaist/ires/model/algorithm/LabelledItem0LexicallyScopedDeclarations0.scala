package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._
import scala.collection.immutable.{ Map, HashMap, HashSet }

object LabelledItem0LexicallyScopedDeclarations0 {
  val length: Int = 0
  val func: Func = Func("""LabelledItem0LexicallyScopedDeclarations0""", List(Id("""this"""), Id("""Statement""")), None, IReturn(EList(List())).setId(36336))
  val instToStepMap: Map[Int, Int] = Map(36336 -> 0, 36337 -> 0)
  val stepToInstsMap: Map[Int, HashSet[Int]] = Map(0 -> HashSet(36336, 36337))
  /* Beautified form:
  "LabelledItem0LexicallyScopedDeclarations0" (this, Statement) => return (new [])
  */
}
