package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._
import scala.collection.immutable.{ Map, HashMap, HashSet }

object LabelledStatement0TopLevelLexicallyScopedDeclarations0 {
  val length: Int = 0
  val func: Func = Func("""LabelledStatement0TopLevelLexicallyScopedDeclarations0""", List(Id("""this"""), Id("""LabelIdentifier"""), Id("""LabelledItem""")), None, IReturn(EList(List())).setId(36351))
  val instToStepMap: Map[Int, Int] = Map(36352 -> 0, 36351 -> 0)
  val stepToInstsMap: Map[Int, HashSet[Int]] = Map(0 -> HashSet(36351, 36352))
  /* Beautified form:
  "LabelledStatement0TopLevelLexicallyScopedDeclarations0" (this, LabelIdentifier, LabelledItem) => return (new [])
  */
}
